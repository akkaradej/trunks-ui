{
  "name": "trunks-ui",
  "version": "0.7.0",
  "private": true,
  "main": "./dist-lib/trunks.scss",
  "engines": {
    "bun": ">=1",
    "pnpm": "use bun",
    "yarn": "use bun"
  },
  "scripts": {
    "dev": "cd ./web && bun start",
    "build": "gulp",
    "pack_lib": "npm pack ./dist-lib",
    "package": "npm run build && npm run pack_lib",
    "publish": "npm publish ./dist-lib"
  },
  "dependencies": {
    "bulma": "1.0.1"
  },
  "devDependencies": {
    "del": "^5.1.0",
    "gulp": "^4.0.2"
  }
}
