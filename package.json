{
  "name": "trunks-ui",
  "version": "0.8.1",
  "private": true,
  "main": "./lib/_index.scss",
  "scripts": {
    "dev": "cd ./web && bun start",
    "build:gh-pages": "cd ./web && bun run build:gh-pages",
    "build_lib": "gulp",
    "pack_lib": "npm pack ./dist-lib",
    "package": "npm run build_lib && npm run pack_lib",
    "publish": "npm publish ./dist-lib"
  },
  "dependencies": {
    "bulma": "1.0.1"
  },
  "devDependencies": {
    "del": "^5.1.0",
    "gulp": "^4.0.2"
  }
}